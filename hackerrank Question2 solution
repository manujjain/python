from collections import Counter

def customSort(arr):
	Countfrequency=Counter(arr)
	sorted_array=sorted(arr,key=Countfrequency.get,reverse=False)
	for items in sorted_array:
		print(items)



if __name__ == "__main__":
	array= [int(x) for x in input().split()]
	arr=customSort(array)
	for items in arr:
	    print(items)
	

